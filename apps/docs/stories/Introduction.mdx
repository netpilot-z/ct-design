import { Meta } from "@storybook/blocks";

<Meta title="Introduction" />

# ct-design Storybook

This Storybook documents the reusable UI layer in `@ct-design/ui` and the
foundation packages behind it.

## Available packages

- `@ct-design/ui`
- `@ct-design/tokens`
- `@ct-design/icons`
- `@ct-design/request`
- `@ct-design/utils`

## Current goals

- Document reusable UI primitives
- Validate the `components -> antd adapter` structure
- Validate component usage in isolation
- Provide a scalable docs entry for future design system work
